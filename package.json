{
  "name": "jenkins-playwright-test",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test --reporter=html --output=./test-results"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
